import http from 'node:http';import {readFile,stat} from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..'),port=Number(process.env.PORT??8080),mime={'.html':'text/html; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8'};http.createServer(async(req,res)=>{try{const rel=req.url==='/'?'apps/demo/index.html':decodeURIComponent(req.url.split('?')[0]).replace(/^\/+/,''),p=path.resolve(root,rel);if(!p.startsWith(root)){res.writeHead(403);return res.end('forbidden');}const st=await stat(p),f=st.isDirectory()?path.join(p,'index.html'):p,d=await readFile(f);res.writeHead(200,{'content-type':mime[path.extname(f)]??'application/octet-stream','cache-control':'no-store'});res.end(d);}catch{res.writeHead(404);res.end('not found');}}).listen(port,()=>console.log(`Chem Lab demo: http://localhost:${port}`));
